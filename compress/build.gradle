plugins {
    id 'java'
}

ext['indeed.publish.name'] = 'util-compress'

dependencies {
    compile project(':util-core')
    compile library('org.apache.logging.log4j:log4j-core')
    compile library('org.apache.logging.log4j:log4j-api')

    testCompile library('junit:junit')
}
